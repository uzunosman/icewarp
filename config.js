// API ve uygulama konfigürasyonları
const CONFIG = {
    // API endpoint
    API_URL: 'http://localhost/icewarpapi/',

    // Uygulama ayarları
    APP: {
        VERSION: '23.0.1',
        PLATFORM: 'windows'
    },

    // Domain ayarları
    DOMAIN: 'api.test',

    // Toast bildirimleri için ayarlar
    TOAST: {
        AUTO_HIDE: true,
        DELAY: 3000,
        POSITION: 'top-end'
    },

    // Textarea ayarları
    TEXTAREA: {
        ROWS: 8,
        FONT_FAMILY: 'monospace'
    }
}; 